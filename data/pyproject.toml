[project]
authors = [{name = "Giò Diani", email = "giodi@users.noreply.github.com"}]
dependencies = ["polars>=1.43.1,<2"]
name = "data"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
data = { path = ".", editable = true }

[tool.pixi.tasks]

[project]
authors = [{name = "Giò Diani", email = "giodi@users.noreply.github.com"}]
dependencies = ["duckdb>=1.5.5,<2", "polars>=1.43.1,<2"]
name = "glamhack26"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
glamhack26 = { path = ".", editable = true }

[tool.pixi.tasks]
